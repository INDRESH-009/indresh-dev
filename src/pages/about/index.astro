---
const IMG = "https://img.indresh.dev";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const projects = await getCollection("projects");
projects.sort((a: typeof projects[number], b: typeof projects[number]) => (b.data.started ?? "").localeCompare(a.data.started ?? ""));
---
<BaseLayout title="Projects · indresh.dev">
  <h1>About</h1>

    <img
      src={`${IMG}/home/profile.jpeg`}
      alt="Profile"
      style="width: 220px; border-radius: 16px; margin: 1rem 0;"
    />

    <p>
      Hi, I'm Indresh. I work at the intersection of computer vision,
      representation learning, and scientific curiosity.
    </p>

    <p>
      This site is where I document experiments, research notes,
      and long-term explorations.
    </p>
  </div>
</BaseLayout>
